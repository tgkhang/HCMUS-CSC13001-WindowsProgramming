<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="POS_For_Small_Shop.Views.Inventory.ReadIngredientPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POS_For_Small_Shop.Views.Inventory"
    xmlns:models="using:POS_For_Small_Shop.Data.Models"
    xmlns:converters="using:POS_For_Small_Shop.Converters"
    xmlns:viewmodels="using:POS_For_Small_Shop.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:CurrencyFormatter x:Key="CurrencyFormatter"/>
    </Page.Resources>

    <Grid Padding="20">
        <StackPanel Spacing="10">
            <TextBlock Text="Ingredient Details" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Name:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedIngredient.IngredientName}" FontSize="20"/>

            <TextBlock Text="Stock:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedIngredient.Stock}" FontSize="16"/>

            <TextBlock Text="Unit:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedIngredient.Unit}" FontSize="16"/>

            <TextBlock Text="Price:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedIngredient.PurchasePrice, Converter={StaticResource CurrencyFormatter}}" FontSize="16"/>

            <TextBlock Text="Supplier:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedIngredient.Supplier}" FontSize="16"/>

            <TextBlock Text="Expiry Date:" FontWeight="Bold"/>
            <TextBlock Text="{Binding SelectedIngredient.ExpiryDate}" FontSize="16"/>

            <Button Content="Back" Click="Back_Click" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>

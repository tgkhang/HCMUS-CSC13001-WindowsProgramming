<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="POS_For_Small_Shop.Views.ShiftPage.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POS_For_Small_Shop.Views.ShiftPage"
    xmlns:viewmodel="using:POS_For_Small_Shop.ViewModels.ShiftPage"
    xmlns:models="using:POS_For_Small_Shop.Data.Models"
    xmlns:converter="using:POS_For_Small_Shop.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <!-- Currency Converter -->
        <converter:CurrencyFormatter x:Key="currency" />

        <!-- MenuItem Template -->
        <DataTemplate x:Key="MenuItemTemplate" x:DataType="models:MenuItem">
            <Border Padding="10" CornerRadius="10" BorderBrush="Gray" BorderThickness="1" Margin="5">
                <StackPanel HorizontalAlignment="Center">
                    <Image Source="{Binding ImagePath}" Width="100" Height="100" Stretch="Uniform"/>
                    <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,5,0,0" />
                    <TextBlock Text="{Binding SellingPrice, Converter={StaticResource currency}}" Foreground="DarkGreen"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Page.Resources>


    <Grid Padding="20">
        <GridView ItemsSource="{Binding MenuItems}"
                  ItemTemplate="{StaticResource MenuItemTemplate}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  IsItemClickEnabled="True"
                  SelectionMode="None"/>
    </Grid>
</Page>

